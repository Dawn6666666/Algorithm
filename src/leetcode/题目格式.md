# LeetCode 题目代码模板

生成新的 LeetCode 题目 Java 类时，**不需要题目描述、示例、提示和约束**，只保留代码模板即可。

## 代码模板

文件路径：`src/leetcode/P0000_Sample.java`（生成时替换为实际题号和题名）

```java
package leetcode;

public class P0000_Sample {
    public static void main(String[] args) {
        Solution solution = new Solution();
        // 如需要，可在这里补充简单测试代码
    }

    //leetcode submit region begin(Prohibit modification and deletion)
    static class Solution {
        public int sampleMethod(int[] nums) {
            // TODO: 请在此处编写算法实现
            return 0;
        }
    }
    //leetcode submit region end(Prohibit modification and deletion)
}
```

## 生成要求

1. 直接生成上述代码模板，不需要题目描述内容。
2. 保持类名格式为 `P${questionId}_${englishTitle}`，生成时将示例类名替换为实际值。
3. 保留 `//leetcode submit region ...` 标记。
4. 如果没有额外测试需求，`main` 方法可以只保留空模板。
